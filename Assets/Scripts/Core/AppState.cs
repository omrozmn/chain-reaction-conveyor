namespace ChainReactionConveyor.Core
{
    /// <summary>
    /// Application state enum for game flow
    /// </summary>
    public enum AppState
    {
        Boot,
        MainMenu,
        Loading,
        Playing,
        Paused,
        Win,
        Fail
    }
}
